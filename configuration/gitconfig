[user]
	email = cn@das-lab.net
	name = Carpe Noctem
[giggle]
	history-view-vpane-position = 510
	file-view-vpane-position = 658
	main-window-maximized = true
	main-window-geometry = 0x0+0+0
	main-window-view = HistoryView

[color]
        ui = auto
[color "branch"]
        current = yellow reverse
        local = yellow
        remote = green
[color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold
        whitespace = red reverse
[color "status"]
        added = yellow
        changed = green
        untracked = cyan

#Highlight whitespace in diffs
[core]
        whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol

[merge]
	tool = meld

[alias]
	st = status
	ci = commit
	br = branch
	co = checkout
	df = diff
	lg = log -p

[push]
        default = tracking
